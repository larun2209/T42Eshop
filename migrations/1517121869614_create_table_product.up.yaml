- args:
    sql: CREATE TABLE "product"("prod_id" integer NOT NULL, "prod_name" text NOT NULL,
      "prod_desc" text NOT NULL, "prod_category" text NOT NULL, "prod_price" integer
      NOT NULL, PRIMARY KEY ("prod_id") )
  type: run_sql
- args:
    name: product
  type: add_existing_table_or_view
